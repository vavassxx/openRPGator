-- Невидимая "сущность-дирижёр" (19,19): глобальный таймер, без действий на себя.
-- Показывает, что engine.on_tick работает вне привязки к конкретному игроку.
engine.on_tick(function(t)
  if t > 0 and t % 120 == 0 then
    engine.notify("Над городом проплывают облака. Тик #" .. t)
  end
end)